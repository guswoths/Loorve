package com.loorve.ui.theme

import androidx.compose.ui.graphics.Color

// ── 배경 계열 ──
val Background       = Color(0xFF0A0A0F)
val Surface          = Color(0xFF12121A)
val SurfaceVariant   = Color(0xFF1A1A27)

// ── 액센트 ──
val Primary          = Color(0xFF7C3AED)
val PrimaryVariant   = Color(0xFF9D5CF0)
val Secondary        = Color(0xFFEC4899)
val Tertiary         = Color(0xFF06B6D4)

// ── 텍스트 ──
val OnBackground     = Color(0xFFF1F5F9)
val OnSurface        = Color(0xFFCBD5E1)
val OnSurfaceVariant = Color(0xFF64748B)

// ── 상태 ──
val Error            = Color(0xFFEF4444)
val Success          = Color(0xFF10B981)

// ── 그라디언트 ──
val GradientStart    = Color(0xFF7C3AED)
val GradientEnd      = Color(0xFFEC4899)