package com.loorve.ui.theme

import androidx.compose.ui.graphics.Color

// ── 배경 계열 ──
val Background       = Color(0xFF0F0F14)
val Surface          = Color(0xFF1A1A24)
val SurfaceVariant   = Color(0xFF22222E)

// ── 액센트 ──
val Primary          = Color(0xFFA78BFA)   // 라벤더 보라 (목업 포인트 컬러)
val PrimaryVariant   = Color(0xFFBDA6FC)   // 밝은 보라 (Pressed/호버)
val Secondary        = Color(0xFFEC4899)   // 핑크 (기존 유지)
val Tertiary         = Color(0xFF06B6D4)   // 시안 (기존 유지)

// ── 텍스트 ──
val OnBackground     = Color(0xFFFFFFFF)   // 헤더, 주요 제목
val OnSurface        = Color(0xFFE2E2F0)   // 본문 텍스트
val OnSurfaceVariant = Color(0xFF8B8B9E)   // 보조/힌트 텍스트

// ── 상태 ──
val Error            = Color(0xFFEF4444)
val Success          = Color(0xFF10B981)

// ── 구분선 ──
val Divider          = Color(0xFF2A2A38)

// ── 광고 플레이스홀더 ──
val AdBackground     = Color(0xFF16161F)

// ── 그라디언트 ──
val GradientStart    = Color(0xFFA78BFA)
val GradientEnd      = Color(0xFFEC4899)